Run the plotting script to generate `imgs/data_plot.png`.

PowerShell (pwsh):

```powershell
python -m pip install --user matplotlib
python scripts/plot_data.py
```

If `python` points to a different interpreter, replace with the full path to your Python executable.
